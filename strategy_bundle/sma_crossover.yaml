type: object
title: MeanReversionStrategyParameters
additionalProperties: false

required:
  - fast_period
  - slow_period

properties:
  fast_period:
    type: int
    default: 10
    description: "SMA lookback for the fast line"
  slow_period:
    type: int
    default: 30
    description: "SMA lookback for the slow line"

risk:
  max_order_notional:
    value: 2000.0
    currency: USD